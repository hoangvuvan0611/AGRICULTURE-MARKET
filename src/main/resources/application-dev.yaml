spring:
  config:
    activate:
      on-profile: dev


  datasource:
    url: jdbc:postgresql://localhost:5432/AGRI_MARKET
    username: postgres
    password: Hoanglam06112003@
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    open-in-view: true
server:
  port: 8081
  servlet:
    context-path: /api/v1